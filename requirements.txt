# Runtime dependencies (training service)
# Versiones pineadas para reproducibilidad. Regenerar con:
#   pip freeze | grep -iE "^(pandas|numpy|scikit-learn|lightgbm|xgboost|openpyxl|optuna|mlflow|matplotlib|plotly|joblib)=="

pandas==2.2.3
numpy==2.2.3
scikit-learn==1.6.1
lightgbm==4.5.0
xgboost==3.2.0
openpyxl==3.1.5
optuna==4.8.0
mlflow==3.12.0
matplotlib==3.10.6
plotly==6.3.0
joblib==1.5.2
boto3>=1.38,<2

# GAMM Phase 0 (corrector de residuos sobre champion). Requeridos por
# scripts/validate_gamm_residuals.py (a crear cuando se arranque Phase 0).
# statsmodels.MixedLM hace random effects + fixed; patsy genera el design
# matrix de splines (bs(x, df=N)). Son C-extensions chicas, instalan rapido.
statsmodels>=0.14.4,<0.15
patsy==1.0.1
