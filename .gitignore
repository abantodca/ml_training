# Secretos / config local
.env
.env.infra
*.pem

# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
.eggs/
*.egg-info/

# Datos (los reales viven en S3)
data/training/*.xlsx
data/processed/

# Salidas del pipeline
artifacts/
reports/
logs/

# Legacy: el backend MLflow ahora es Postgres (Docker), `mlruns/` ya no se usa.
# La regla queda por si alguien volvio de una rama vieja con la carpeta en disco.
mlruns/

# Editores
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code (config local del agente)
.claude/
